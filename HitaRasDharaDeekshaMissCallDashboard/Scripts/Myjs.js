    var showPopup = function (code) {
        debugger;
        switch (code) {
        case 1:
            swal('User already exists', 'You can update the status if required from Update Status page!', 'error');
            break;
        }
    };

